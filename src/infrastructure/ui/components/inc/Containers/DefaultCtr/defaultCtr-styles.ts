import tw, { styled } from 'twin.macro'

export const StyledDefaultCtr = styled.div`
    ${tw`px-1 mx-2 max-w-6xl`}
    ${tw`sm:px-2 sm:mx-auto`}
    ${tw`sm:px-3`}
    ${tw`md:px-5`}

    ${tw`bg-primary`}
`
