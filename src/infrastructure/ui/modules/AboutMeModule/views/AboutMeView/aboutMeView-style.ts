import tw, { styled } from 'twin.macro'

export const StyleAboutMeView = styled.div`
    ${tw`min-h-[100vh] flex justify-center items-center`}
`

export const StyledCardProfile = styled.section`
    ${tw`mt-5 w-full`}
    ${tw`md:mt-0 md:flex md:flex-row`}
`
